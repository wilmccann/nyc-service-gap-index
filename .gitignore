# Databricks bundle deployment artifacts
.databricks/
.bundle/

# Dashboard workspace artifacts (.lvdash.json are imported as dashboard objects,
# not plain files — use *_dashboard.json for the DAB instead)
*.lvdash.json
# macOS and Word lock files
.DS_Store
~$*

# The two hackathon CSVs are committed once, in hackathon-materials/.
# scripts/stage_data.sh copies them here for the bundle; don't commit the copies.
resources/data/rat_sightings.csv
resources/data/restaurant_inspections.csv
